{{#navigation}}
{{#unless subtitle}}
<li><a href="{{{link}}}"><span>{{title}}</span></a></li>
{{/unless}}
{{#if subtitle}}
<li class="dropdown"> <a data-toggle="dropdown" class="dropdown-toggle" href="{{{link}}}">{{title}}<span class="caret"></span></a>
	<ul class="dropdown-menu">
	{{#subtitle}}
		{{#if subtitle}}
		<li class="dropdown-submenu"><a href="{{{link}}}">{{title}} <i class="fa fa-angle-right"></i></a>
			<ul class="dropdown-menu">
			{{#subtitle}}
				<li><a href="{{{link}}}">{{title}}</a></li>
			{{/subtitle}}
			</ul>
		</li>
		{{else}}
		<li><a href="{{{link}}}">{{title}}</a></li>
		{{/if}}
	{{/subtitle}}
	</ul>
</li>
{{/if}}
{{/navigation}}
